#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - returns the length of a string
 * @s: function parameter
 * Return: 0
 */

int _strlen_recursion(char *s)
{
	int n = strlen(s);

	return (n);
}
