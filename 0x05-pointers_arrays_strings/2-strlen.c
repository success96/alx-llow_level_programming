#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 *
 * @s: function parameter
 *
 * Return: 0
 */

int _strlen(char *s)
{
	int first = strlen(s);

	return (first);
}
