#include "main.h"
#include <string.h>

/**
 * _strcpy - copies strings
 * @dest: first para
 * @src: second para
 * Return: 0
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return (dest);
}
